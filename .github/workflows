name: Keep Streamlit App Alive

on:
  schedule:
    - cron: '*/30 * * * *'  # every 30 minutes

jobs:
  ping-app:
    runs-on: ubuntu-latest
    steps:
      - name: Curl to keep alive
        run: curl --silent https://knowledge-chatbot-he4ulz25jg7xkmaxahdwnv.streamlit.app/
